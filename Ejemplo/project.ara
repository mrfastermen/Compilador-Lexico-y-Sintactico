inicio

entero num = 10, suma = 0;

circulo(50,150,80);
circulo(50,250,80);

suma = (circulo + circulo);

para li hasta num hacer
	funcion hacecirculo(num){
		circulo(150,50,80);
		casa(aux, x);
	}
finciclo

si linea == num entonces
	elipse(0,360,200,100,700,600);
	rectangulo(150, 50, 100, 100);
finsi
	sino
	mientras circulo >= num hacer
		casa(aux, x);
	finciclo
	finsino
		sino
			retornar false;
		finsino

funcion casa(aux, x){
	linea(0,350,200,350);
}

fin
